spring:
  application:
    name: forum-post
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: dev
        file-extension: yml
        auto-refresh: true
        bootstrap:
          enable: true